// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    firebase: {
        apiKey: 'AIzaSyAfehkHO8oNZnTIR-Lq_MSHfLoaeNox73k',
        authDomain: 'hacka-food-registry.firebaseapp.com',
        databaseURL: 'https://hacka-food-registry.firebaseio.com',
        projectId: 'hacka-food-registry',
        storageBucket: '',
        messagingSenderId: '551345699150',
        appId: '1:551345699150:web:b54329362ab6391bb1c61f'
    },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
